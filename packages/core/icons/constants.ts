import { ComponentId } from "../components/constants"
import { IconSetId } from "./types"

/**
 * Default icon set ID used when creating new icon sets
 */
export const ICON_SET_DEFAULT_ID: IconSetId = "google-material"

/**
 * ComponentId for the IconSet board
 * All icon sets share the same board
 */
export const ICON_SET_BOARD_ID = ComponentId.ICON_SET
