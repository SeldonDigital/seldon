import { seldonAvailableIconIds } from "./available"

/**
 * Seldon enables its entire set by default, so the default-on subset equals the
 * full available set. Re-exported under the parity name the other sets use.
 */
export const seldonDefaultEnabledIconIds = seldonAvailableIconIds
