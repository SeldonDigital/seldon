/**
 * Identifies a shipped icon component set under `sets/`. Each source maps to an
 * icon id prefix, such as `material-`, `carbon-`, `lucide-`, and `seldon-`.
 */
export type IconSetId = "google-material" | "carbon" | "lucide" | "seldon"
