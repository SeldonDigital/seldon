import type { Workspace } from "../../../../index"

/** Reserved until `font-collections` / `icon-sets` / `media` spec is finalized. */
export function stubsResourceMapNoop(workspace: Workspace): Workspace {
  return workspace
}
