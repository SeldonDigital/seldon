import { ComponentId } from "../../../components/constants"

// List of components that have updated ids
export const updatedComponentIds: Record<string, ComponentId> = {
  headerPanels: ComponentId.BAR_HEADER,
  headerActions: ComponentId.HEADER_ACTION,
  headerCards: ComponentId.HEADER_CARD,
}
