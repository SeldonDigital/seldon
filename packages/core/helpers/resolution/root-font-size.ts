/**
 * The CSS root font size, in pixels, that a `rem` length resolves against. The
 * factory emits `html { font-size: 16px }`, so a rem value times this constant
 * is the size the browser renders.
 */
export const ROOT_FONT_SIZE_PX = 16
