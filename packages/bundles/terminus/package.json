{
  "name": "@seldon/terminus",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Headless Seldon engine bundle: the workspace engine (@seldon/core) plus the export factory (@seldon/factory).",
  "license": "SEE LICENSE IN ../../../license/noncommercial/LICENSE.md",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SeldonDigital/seldon.git",
    "directory": "packages/bundles/terminus"
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=22"
  },
  "sideEffects": false,
  "exports": {
    ".": {
      "node": {
        "types": "./dist/index.d.ts",
        "development": "./index.ts",
        "default": "./dist/index.js"
      },
      "default": "./index.ts"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "index.ts",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prepublishOnly": "echo 'Publishing @seldon/terminus is blocked. See PUBLISHING.md to unlock.' && exit 1"
  },
  "dependencies": {
    "@seldon/core": "0.0.0",
    "@seldon/factory": "0.0.0"
  }
}
