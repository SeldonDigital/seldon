/**
 * @seldon/terminus - the headless Seldon engine.
 *
 * Re-exports the workspace engine (`@seldon/core`) and the export factory
 * (`@seldon/factory`) as one install. Use it to load, edit, and export a
 * workspace from a consumer project with no editor. See the package README for
 * the load/edit/export flow.
 */
export * from "@seldon/core"
export * from "@seldon/factory"
