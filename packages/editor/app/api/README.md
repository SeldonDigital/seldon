# API routes (fork extension point)

- `app/api/debug/log/route.ts` — receives client debug logs when Help → Enable Debug Mode is on (mirrors hosted editor).
- Add `app/api/export/route.ts` for server-side factory export.
