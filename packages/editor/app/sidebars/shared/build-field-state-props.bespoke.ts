// BESPOKE: editing-field outline for inline rename/value inputs. The generated
// input has no editing leaf state yet, so the editor applies this inline. Uses
// the permanent chrome --hairline and the generated --sdn-swatch-active token.
// Remove once a generated input View covers the editing state.
export const EDITING_OUTLINE = "var(--hairline) solid var(--sdn-swatch-active)"
