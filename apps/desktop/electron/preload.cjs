// The shell exposes nothing to the renderer yet. The editor runs as a plain web
// app. Add a contextBridge surface here when the desktop build needs native
// capabilities (file dialogs, menus, deep links).
