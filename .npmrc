# Scope @seldon to a local Verdaccio registry for publish and install. Every other
# package still resolves from the public npm registry. Workspace packages are
# always resolved locally, so this does not affect the monorepo's own install.
# Change this line to publish to public npm later.
@seldon:registry=http://localhost:4873/
