node_modules
dist
/build
coverage/
/cypress/snapshots/actual
/cypress/snapshots/diff
/.pnp
.pnp.js
*.tsbuildinfo

logs
*.log
npm-debug.log*

.DS_Store
*.pem

.eslintcache
.vscode
!.vscode/launch.json

# Local Verdaccio registry storage and credentials
.verdaccio-storage
htpasswd
.htpasswd

# Cursor editor swap files
*.crswap

# Keep environment variables out of version control
.env
.env.local
*.local

.vercel
__exports__

# Self-hosted font woff2 files + licenses, materialized by scripts/generate-fonts.mjs
# into the shared public folder both editors serve.
/packages/editor/shared/public/font-files
/packages/editor/shared/public/font-licenses

# Live filesystem workspace store used by both editors. The committed design
# source at `.seldon/<label>.<framework>.json` stays tracked; only the live
# store under `workspaces/` is ignored.
.seldon/workspaces/
